export const wrongReplyBaseMessage = `Wrong command.
Available commands:
`;

export const startCmd = `/start - start registration (only for new users)`;
export const continueCmd = `/continue - continue registration process`;
export const menuCmd = `/menu - main menu`;
